

 Spring Standalone Collection
    1. A collection (List, Set, Map, or Properties) managed by Spring.
    2. Injected into a Spring bean via XML or Java configuration.
    3. Standalone means it is defined directly in Spring, without external frameworks.
    4. Useful for storing multiple values and injecting them into beans.

